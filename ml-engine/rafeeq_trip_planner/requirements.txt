# ── Rafeeq Trip Planner — Production Dependencies ─────────────────────────────
# Pinned to the exact versions tested locally. Do NOT add dev/scraping tools here.

# Web framework
fastapi==0.135.3
uvicorn[standard]==0.44.0       # [standard] pulls in websockets + httptools
starlette==1.0.0
pydantic==2.12.5
pydantic-core==2.41.5
python-multipart==0.0.27        # required for FastAPI form/file uploads

# HTTP client (used by tests; keep for healthcheck utilities)
httpx==0.28.1
anyio==4.13.0

# ML / data pipeline
scikit-learn==1.8.0
joblib==1.5.3
numpy==2.4.4
pandas==3.0.2
